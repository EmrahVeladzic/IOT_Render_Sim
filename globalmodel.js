import {Model,AnimationBuffer,JointBuffer,MeshBuffer,ImageBuffer,SoundBuffer} from './types.js'
const globalModel = new Model();

const tempMesh = new MeshBuffer();
const tempJoint = new JointBuffer();
const tempAnimation = new AnimationBuffer();

export {globalModel, tempMesh, tempJoint, tempAnimation}

